export const environment = {
  production: true,
  eventoAppUrl: window["env"]["eventoAppUrl"] || "default",
  eventoRSUrl: window["env"]["eventoRSUrl"] || "default",
  eventoASUrl: window["env"]["eventoASUrl"] || "default"
};
